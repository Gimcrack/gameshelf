NUXT_PUBLIC_API_BASE=http://localhost:8000
# Public GOG OAuth client id (not secret) — same value as api/.env's GOG_CLIENT_ID.
# Required to build a valid GOG login URL (B17/V54).
NUXT_PUBLIC_GOG_CLIENT_ID=
