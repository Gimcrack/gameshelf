# SPEC — GameBower v1

## §G goal

Unified game library dashboard. Aggregate owned games Steam+GOG → dedupe to canonical titles → tag/filter/smart collections → backlog stats. Web only. Core loop: connect → view library → triage → return.

Official name: GameBower (gamebower.com). Was GameShelf — renamed amend 2026-07-15 → T42.

Source: `design-doc.md` v0.1 (pre-rename, historical — ⊥ rewritten).

## §C constraints

- Backend: Laravel 11+, Sanctum stateless token auth.
- Frontend: Nuxt 4 (Vue), JSON over HTTPS. Was Nuxt 3 → upgrade T10.
- Styling: Tailwind CSS. Added amend 2026-07-13 → T11.
- Theme: dark only, ∀ views. Slate base + teal accent motif. ⊥ light mode v1. Palette formalized from `design.png` (Nest Hex sheet) amend 2026-07-15 → T43: bg near-black `#171A21`, slate surface `#3A4556`, deep indigo `#1A1E4A`, teal accent `#2FA7A0`, violet accent `#7D6BFF` — Tailwind theme tokens. Sheet's light/monochrome logo variants = marketing/export assets only, ⊥ app light mode.
- Landing: 1 public marketing page (route `/welcome`), guests → `/welcome` ≠ `/login`. Same dark slate/teal theme. SPA `ssr: false` holds → SEO limited, accepted v1. Added amend 2026-07-13 → T12.
- DB: MySQL (Eloquent). tags[]/genres[] → JSON columns | pivot tables.
- Queue: Laravel Queue, Redis driver (Redis already in stack) + Scheduler. Daily per-user sync & on-demand "sync now".
- "sync now" throttled per connection (≥5 min gap) + API rate limiting (Sanctum throttle).
- Platform sync (initial + subsequent) auto-queues IGDB sync: ∀ owned game IGDB-stale >24h (`games.igdb_synced_at` null | < now-24h) → queued match (provisional) | refresh (matched) during SyncConnection, fresh (<24h) games skipped. Freshness gate avoids redundant IGDB volume on daily (V5) + "sync now" re-syncs. Fanned out per V39, ⊥ inline. Added amend 2026-07-15 → T51 (V50).
- SPA auth: Sanctum bearer token (stateless per V3). localStorage XSS tradeoff accepted over cookie/CSRF mode.
- Cache: Redis — IGDB lookups, sync status, rate-limit tracking.
- Metadata: IGDB (SteamGridDB ? box art).
- v1 platforms: steam, gog only. Epic/Xbox/PS ⊥ (no public API). + `manual` pseudo-platform (T14): user-added games, synthetic connection, ⊥ sync.
- Xbox: added v2 platform, evaluated 2026-07-16. Real Microsoft OAuth (Azure AD app reg, login.microsoftonline.com) → XBL user token → XSTS token 3-hop exchange (mirrors community `xbox-webapi`/`xboxreplay` pattern) — same OAuth trust class as existing Steam/GOG, ⊥ password/session-cookie storage. Library surface = Xbox Live `titlehub` title-history (games w/ Live activity/achievements present) ≠ full Microsoft Store purchase history (⊥ exposed via any Xbox Live API) — caveat, mirrors GOG's playtime-unavailable honesty. Endpoints undocumented/community-reverse-engineered, 3rd-party consumer use ⊥ officially sanctioned outside ID@Xbox/Creators partner program → maintenance + ToS-standing risk, same class as GOG. Added amend 2026-07-16 → T63 (V60).
- PlayStation + Humble Bundle: evaluated 2026-07-16, ⊥ added. Both require a password-equivalent session token to read library — PSN NPSSO (Sony's own docs: "equivalent to your password") | Humble Bundle scraped session cookie/stored credentials (⊥ OAuth, ⊥ any API at all) — same heavier-trust-model class V58 already rejected for Steam family sharing's ruled-out session-token route. Rejected on trust-model grounds, ⊥ technical infeasibility (V60) — revisit only if either platform ships real OAuth for 3rd-party library reads.
- Discovery: IGDB = catalogue source. ∀ discover calls server-proxied, Redis-cached, ≤ 4 req/s shared budget (V4 throttle). ⊥ FE direct IGDB. Added amend 2026-07-13 → T14,T15.
- Tags: Steam has ⊥ public API for community tags → platform taxonomy = IGDB only (themes/keywords/game_modes, mirrors existing genres). User freeform tags (T7) unaffected. Added amend 2026-07-15 → T22.
- Steam Deck: ⊥ official API for compat rating (Verified/Playable/Unsupported/Unknown) → undocumented store endpoint, same risk class as GOG (maintenance risk, unofficial). Refetched every sync (matches playtime cadence) → 1 extra HTTP call per Steam owned_game per sync; ? revisit w/ TTL cache if this trips Steam's aggressive unauth store rate-limit (same class already flagged on `appName`) at larger library sizes. Steam-only — GOG/manual rows ⊥ rating. Added amend 2026-07-15 → T26.
- Steam Family Sharing: library_status=shared (V58) for games playable via a manually-added family member's shared library. Detection = public `GetOwnedGames` for that member's SteamID (reuses existing Steam ownership fetch, no new endpoint) cross-referenced vs `appdetails.categories` id=62 ("Family Sharing", public, unauthenticated) — best-effort heuristic, ⊥ guaranteed live entitlement (Valve doesn't expose post-purchase sharing revocation). ⊥ IFamilyGroupsService/live-session route — research spike 2026-07-16 found it needs a Steam web-session access_token (not an API key), i.e. storing the user's actual Steam login session; rejected, breaks connection trust model (no other connection stores login credentials). Family members added manually (steam_id/vanity resolve+confirm, mirrors V25), backed by synthetic `platform_connections` row per member (platform=steam_family, mirrors `manual` T14/V19 pattern) — no tokens, reuses existing sync/reconcile machinery. Requires family member's profile + game details public (V15-class failure mode if private). GOG/manual ⊥ shared (Steam-only surface). Added amend 2026-07-16 → T60.
- ⊥ v1: social, price tracking, native mobile.
- API platform-agnostic from day 1 → iOS client additive later.
- GOG API undocumented (community docs: gogapidocs) → maintenance risk.
- GOG playtime unavailable via owned-games API → playtime nullable, null = unknown ≠ 0.
- Burn-down pace: `playtime_snapshots` table, row per sync. Steam `playtime_2weeks` ? seed until history accrues.
- "Quick wins" conditional: IGDB time-to-beat where present; games w/o data excluded from collection.
- Auth: own accounts — email/password + Sanctum tokens. Steam OpenID ⊥ identity (connection only).
- Brand: GameBower, prod domain gamebower.com. Was GameShelf — renamed amend 2026-07-15 → T42. Local dev stays `gameshelf.test` Herd site + repo dir `gameshelf` (⊥ relink — B1 risk class, rename deliberate later if ever). FE auth storage/state keys `gameshelf_*` → `gamebower_*` — existing beta sessions invalidated (re-login), accepted. Logo system "Nest Hex" (`design.png`) amend 2026-07-15 → T43: brandmark = nested hexagons + teal inner alcove; wordmark = "Game" heavy indigo/white + "Bower" lighter slate w/ teal "w"; lockup/app-icon/favicon variants. Assets delivered 2026-07-15 → `gamebower-svg-assets/` (8 SVGs + README), copied to `web/public/brand/` @ T43. T43 unblocked. Gaps + resolutions: ⊥ standalone on-dark brandmark (color variant = indigo/slate strokes, invisible on `#171A21`) → derive by recolor, paths verbatim, colors ← supplied app-icon on-dark treatment (white + `#AAB4C3` + teal); favicon ⊥ dark-circle variant → wrap supplied mark in `#1A1E4A` circle. Wordmark SVG = non-outlined `<text>` (font-dependent) + teal chevron overlays text's own "w" → app header = styled HTML text ("Game"+"Bower", teal "w"), SVG wordmark/lockup marketing-only. ? wordmark font — Inter fallback stack per README, keep current font stack v1.
- Hosting: Laravel Forge + single VPS (API, queue, Redis, DB). Nuxt SPA static via same box | CDN. Prod domain gamebower.com (T42).
- Nuxt mode: SPA (`ssr: false`). All views behind auth, no SEO need. Holds in Nuxt 4 — upgrade ⊥ drop `ssr: false`.
- FE facet filters: ∀ render via FacetFilter → searchable multi-select combobox (`@headlessui/vue` Combobox), regardless of option count. Added amend 2026-07-15 → T33 (was > 5 → combobox, ≤ 5 → checkbox); threshold dropped amend 2026-07-15 → T35. Also ESRB + Steam Deck status → FacetFilter — amend 2026-07-15 → T36. Deck status options = static list (⊥ facets endpoint); ESRB options ← facets `esrb_ratings[]`, only values present in caller library (was static E|E10|T|M|AO|RP list); `none` sentinel ∈ options when ∃ unrated (null) games → FE displays "No Rating" — amend 2026-07-15 → T36. Static-list FacetFilters also: library_status (owned|free|wishlist|none → T38, `shared` added amend 2026-07-16 → T60), personal rating (1-5 + `none` → "Unrated", star labels → T40) — amend 2026-07-15.
- Local dev topology: API via Herd → `http://gameshelf.test` (Sites symlink → `api/`), FE `nuxt dev` :3000, `web/.env` NUXT_PUBLIC_API_BASE=http://gameshelf.test. API unreachable → browser reports CORS-shaped error ∴ check server up before touching CORS config. Laravel CORS = framework defaults (ACAO *), no cors.php published.

## §I interfaces

- api: `POST /api/connections` {platform: steam|gog} → OAuth/connect flow
- api: `POST /api/connections/:id/sync` → 202, dispatch sync job
- api: `GET /api/connections` → [{platform, last_synced_at, status}]
- api: `DELETE /api/connections/:id` → 200 soft disconnect: status → disconnected, owned_games kept (V13). Added T5.
- api: `GET /api/library` → deduped games, owned-on platforms per game (each platform entry carries `deck_status` nullable, Steam-only, T26); sort: alpha|playtime|last_played|added; filter: platform|genre|theme|keyword|game_mode|status|tags|playtime range|collection (system slug | custom id, explicit params win)|`deck_status[]` (multi-select verified|playable|unsupported|unknown, matches ∀ game w/ ≥1 owning platform row in listed set)|`esrb[]` (multi-select; values = stored IGDB labels verbatim, opaque — e.g. E|E10+|T|M — ⊥ hardcoded enum, unknown label matches ∅ ⊥ 422 (B10/V43); + `none` sentinel matches `esrb_rating` IS NULL; was single-select w/ assumed E|E10|T|M|AO|RP vocab, extended T36, vocab fixed B10)|multiplayer|coop|local_multiplayer|local_coop (bool)|`library_status[]` (multi-select owned|free|shared|wishlist|none, T38, `shared` added T60)|`rating[]` (multi-select 1|2|3|4|5|none — `none` = unrated null, T40)|`q` (title substring, case-insensitive). Entries carry meta (status, status_declared, tags, notes, rating, hidden) + time_to_beat_minutes + esrb_rating/multiplayer/coop/local_multiplayer/local_coop + `library_status` (owned|free|shared|wishlist|none, precedence V42/V58, T38/T60). Wishlist/none rows: platforms[] empty, playtime null. Shared rows: platforms[] carries the shared-flagged row, playtime_minutes forced null (V58 — fetched playtime belongs to the family member's account, not caller's). ⊥ hidden entries by default → `include_hidden=1` reveals them. Extended T7, T22 (theme/keyword/game_mode filters), T25 (hidden), T26 (deck_status), T27 (esrb/multiplayer/coop/local flags), T28 (`q`), T38 (library union + library_status), T40 (rating[]), T60 (shared status).
- api: `GET /api/library/facets` → {genres[], themes[], keywords[], game_modes[], platforms[], esrb_ratings[] (T36, + `none` when ∃ null-rated games)} distinct values present across caller's owned games. Hidden games excluded by default (V28). Static full vocabulary — ⊥ responsive to other active filter selections (V36, v1 simplification, ? revisit if users want narrowing facets). Added T28.
- api: `GET /api/library/:game_id` → single entry, same shape as list item | 404 if ∉ caller library. Added T24.
- api: `PUT /api/library/:game_id/meta` {status?, tags[]?, notes?, rating? 1-5, hidden? bool} → upsert partial, 404 if game ∉ caller library. Added T7, extended T25 (hidden).
- api: `POST /api/library/:game_id/rematch` {igdb_id} → repoints entry's canonical game (V34): reuses existing `games` row if igdb_id already matched elsewhere (V7), else creates one; orphaned provisional row deleted iff 0 refs remain (owned+wishlist+meta, V34). Works on provisional AND already-matched games — corrects a wrong auto-match too, ⊥ provisional-only. Operates on whole library union (owned ∪ wishlist ∪ meta-orphans, V42) — repoints owned_games and/or wishlist_items rows; 404 if game_id ∉ caller library union (was owned-only, extended T47/B13). Query-override search for candidates reuses existing `GET /api/discover/search` (I.igdb, T15) — no new IGDB call path. Added T29, extended T47.
- api: `POST /api/library/:game_id/refresh-igdb` → re-fetches current igdb_id's IGDB record (`getGame`), reapplies canonical attrs (genres/themes/keywords/game_modes/esrb_rating/multiplayer flags) + time_to_beat (V35). Synchronous — mirrors existing inline IGDB call in manual-add (T14), ⊥ subject to V8 (that invariant scopes bulk connection sync only). 422 if game has no igdb_id yet (rematch first); 404 if IGDB no longer knows the id. Added T30.
- api: `POST /api/library/sync-igdb` → 202, dispatch bulk IGDB sync job (V38) for caller's whole library UNION (owned ∪ wishlist ∪ meta-orphans, V42/V48 — was owned-only, B15/T50): matches provisional games (owned via ∀ connection + wishlist-only + meta-orphan) + refreshes already-matched games' canonical attrs across the union. Throttled per-user ≥5 min gap (mirrors connection "sync now", §C) → 429 if too soon. No live progress — v1, caller reloads later. Added T31, union-extended T50.
- api: `GET/POST /api/collections` → GET {system: [{slug,name,description}], custom: [...]}; custom entries carry `type: filter|manual`. POST {name, type: filter|manual, filters? (required if type=filter)} → 201, filter keys ⊂ library vocabulary, ⊥ nested collection. Filter-key whitelist (`ALLOWED_FILTER_KEYS`) = full sidebar vocab: + deck_status, esrb, multiplayer, coop, local_multiplayer, local_coop, q (was missing post-T26/T27/T28 params — V44) — amend 2026-07-15 → T44. System: unplayed (playtime=0 | declared unplayed, V12), abandoned (played, untouched ≥6 mo, ≠finished), quick_wins (ttb < 300 min ∧ ttb present). ⊥ hidden games counted in system collections (V28). Shape set T7, extended T23 (type).
- api: `POST /api/collections/:id/games` {game_id} → 201 add to manual collection; `DELETE /api/collections/:id/games/:game_id` → 200 remove. 422 if collection type ≠ manual. Added T23.
- api: `PATCH /api/user` {email? | password? + password_confirmation?, current_password !} → updated user JSON. Added T13.
- api: `GET /api/tokens` → [{id, name, last_used_at, created_at}]; `POST /api/tokens` {name} → 201 {token: plaintext, shown once}; `DELETE /api/tokens/:id` → revoke. Sanctum PATs. Added T13.
- api: `POST /api/library` {igdb_id} → 201 manual owned_game (canonical game reused via V7 | created from IGDB record); duplicate manual add → 200 existing. Added T14.
- api: `DELETE /api/library/:game_id/manual` → remove manual entry only; platform-synced rows untouchable. Added T14.
- api: `GET /api/discover/search` {q} → IGDB search proxy, each hit {igdb_id, title, cover_url, genres[], release_date, rating?, in_library}. Added T15.
- api: `GET /api/discover/browse` {genre?, sort: rating|release|popularity, page} → IGDB catalogue proxy, same hit shape. Added T15.
- api: `GET /api/discover/similar` → rails [{seed: owned game, similar: [hit...]}], seeds = top-playtime/rated owned w/ igdb_id. IGDB `similar_games`. Added T16.
- api: `GET/POST/DELETE /api/wishlist` → save discovered games w/o ownership claim; POST {igdb_id}, hit shape + added_at + per-platform presence flags; promote = POST /api/library + row removed. Added T17.
- api: `POST /api/wishlist/sync` → 202, dispatch wishlist sync job (throttled ≥5 min like connection sync). Added T20.
- ext: Steam `IWishlistService/GetWishlist` → appids, ! public profile (V15-style error else); titles via store `appdetails` (cached, unauthenticated). READ ONLY — Steam wishlist writes impossible via public API. T20.
- ext: GOG `embed.gog.com/user/wishlist.json` read; `user/wishlist/add/{productId}` + `remove/{productId}` write w/ stored OAuth token (V14 refresh applies). 2-way. T20.
- ext: IGDB `external_games` → igdb_id ↔ steam appid | gog product id mapping. IGDB renamed `category` → `external_game_source` (B11/V45, live-verified) — query by `external_game_source`, ids unchanged (source 1 = steam, 5 = gog). Cached (⊥ forever for negatives — V46). T20.
- api: `GET /api/discover/franchises` → [{franchise, owned: [...], missing: [hit...]}] via IGDB franchise data. Edition/remaster noise accepted v1. Added T18.
- api: `GET /api/discover/upcoming` → hits w/ release_date ∈ next 6 mo, filtered by caller's top owned genres. Added T19.
- api: `GET /api/stats/backlog` → {unplayed_count, est_hours, burndown} (avg hrs/wk last N wks → yrs to clear); shareable card view. ⊥ hidden games counted (V28). Extended T25.
- api: `GET /api/connections/steam/resolve` {steam_id|vanity_url} → 200 {steam_id,persona_name,avatar_url} | 404 no player | 422 invalid input. Pure lookup, ⊥ creates connection. Added T21.
- api: `GET /api/family-members` → [{id, steam_id, persona_name, avatar_url, last_synced_at, status}] (status via joined synthetic connection). Added T60.
- api: `POST /api/family-members` {steam_id} → 201, creates synthetic `platform_connections` row (platform=steam_family) + `family_members` row; FE resolves+confirms first via existing `GET /api/connections/steam/resolve` (V25 pattern, no new resolve endpoint). Added T60.
- api: `DELETE /api/family-members/:id` → 200, hard-deletes family_members row + synthetic connection + all `shared` owned_games rows sourced from it (⊥ V13 soft-keep — no real "your data" to preserve). Added T60.
- api: `POST /api/connections/:id/sync` (existing) → SyncConnection branches on platform=steam_family: fetches that member's GetOwnedGames, filters to category-62 (I.ext) + not-already-caller-owned, upserts `owned_games` w/ shared=true, playtime_minutes forced null (V58). No new sync endpoint. Extended T60.
- ext: Steam Web API `GetOwnedGames` — Steam ID + API key; OpenID identity-only, library read via public Web API; returns `playtime_2weeks`. T37: fetched 2× per sync (base + `include_played_free_games=1`) — appid ∈ extended ∖ base = F2P → `owned_games.free_to_play=true` (V41).
- ext: Steam `ResolveVanityURL` — vanity URL → SteamID64 at connect
- ext: Steam `saleaction/ajaxgetdeckappcompatibilityreport` {nAppID} → undocumented, unauthenticated; `resolved_category`: 0=unknown|1=unsupported|2=playable|3=verified. Best-effort (V31), fetched per Steam owned_game per sync. Added T26.
- ext: Steam `appdetails` `categories[]` id=62 ("Family Sharing") → public, unauthenticated, best-effort corroboration signal (⊥ guaranteed live entitlement) for library_status=shared (V58). Reuses existing appdetails cache (T20 store-title fallback plumbing). Added T60.
- ext: Steam `GetOwnedGames` reused per manually-added family member's SteamID (public Web API, same call as caller's own Steam connection) → candidate shared-game list, diffed vs category-62 appdetails + caller's own owned_games (caller ownership always wins, V21/V42-class overlap rule). Added T60.
- ext: IGDB `age_ratings` → `organization` (ESRB=1, verified live B7) + nested `rating_category.rating` (human label string directly, e.g. "M" — ⊥ local id→label table needed) → `games.esrb_rating` nullable string (E|E10|T|M|AO|RP). 1 nested-field request, rides existing canonical fetch (V4 unaffected). Best-effort, fetched once @ match/create (⊥ refetch — rating doesn't change). Added T27. Field names corrected B7/V37 — was `category`+`rating` (wrong, silently dropped by IGDB, ⊥ ever populated).
- ext: IGDB `multiplayer_modes` → per-game rows (campaigncoop/dropin/lancoop/offlinecoop/offlinemax/onlinecoop/onlinemax/splitscreen/splitscreenonline), OR'd across all returned rows → derives `games.multiplayer/coop/local_multiplayer/local_coop` (V32). Best-effort, fetched once @ match/create. Added T27.
- ext: GOG OAuth2 → embedded API for owned games; tokens expire ~1 hr → refresh flow
- ext: Microsoft identity platform OAuth2 (Azure AD app reg, `login.microsoftonline.com`) → MS access token → exchanged `user.auth.xboxlive.com` for XBL3.0 user token → exchanged `xsts.auth.xboxlive.com` (relying party `xboxlive.com`) for XSTS token — 3-hop chain, mirrors community `xbox-webapi`/`xboxreplay` pattern. XSTS token = bearer for Xbox Live API calls. Added T63 (V60).
- ext: Xbox Live `titlehub` (`titlehub.xboxlive.com/users/xuid({xuid})/titles/titlehistory/decoration/...`) → games w/ Xbox Live activity (achievements/stats present) — approximates library, ⊥ full Microsoft Store purchase history (⊥ exposed via any Xbox Live API surface, §C caveat). Added T63.
- ext: IGDB search (title + platform hint) → canonical game record; auth = Twitch OAuth client credentials (app access token), ≤ 4 req/sec
- ext: IGDB `game_time_to_beats` → normally-pace seconds → games.time_to_beat_minutes; best-effort, fail ⊥ fail match. Added T7.
- env: `STEAM_API_KEY`, `TWITCH_CLIENT_ID`, `TWITCH_CLIENT_SECRET`, `GOG_CLIENT_ID`, `GOG_CLIENT_SECRET`, `APP_KEY` (token encryption) ! set. FE (web/): `NUXT_PUBLIC_GOG_CLIENT_ID` — same value as `GOG_CLIENT_ID` (public, not secret) — ! set for GOG login to build a valid authorize URL, amend 2026-07-15 → B17/T56. `XBOX_CLIENT_ID`, `XBOX_CLIENT_SECRET` (Azure AD app reg) ! set — added amend 2026-07-16 → T63.
- db: `users` — id, email, created_at
- db: `platform_connections` — id, user_id, platform enum(steam|gog|manual T14|steam_family T60|xbox T63), external_account_id, auth_token (encrypted), refresh_token (encrypted, nullable), token_expires_at, last_synced_at, status
- db: `family_members` — id, user_id, steam_id, persona_name, avatar_url, platform_connection_id (FK → synthetic steam_family connection, V58), created_at. unique(user_id, steam_id). Added T60.
- db: `games` — canonical, 1 row per real-world game: id, igdb_id (nullable — provisional when unmatched), title, cover_url, genres[], release_date, time_to_beat_minutes (nullable, T7), themes[], keywords[], game_modes[] (nullable JSON, T22), esrb_rating (nullable string, T27), multiplayer/coop/local_multiplayer/local_coop (nullable bool — null = not yet fetched ≠ false, T27), igdb_synced_at (nullable timestamp — last successful IGDB canonical fetch/match/refresh, null = never; drives V50 24h freshness gate, T51)
- db: `collections` — id, user_id, name, type enum(filter|manual) default filter, filters JSON nullable (required iff type=filter). Added T7, type col T23.
- db: `collection_games` — id, collection_id, game_id, added_at. unique(collection_id, game_id). Membership for type=manual collections only. Added T23.
- db: `owned_games` — 1 row per (user, platform, game): id, user_id, platform_connection_id, game_id, platform_game_id, playtime_minutes (nullable), last_played_at, install_status, added_at, deck_status (nullable enum: unknown|unsupported|playable|verified — null = not fetched/inapplicable ≠ Valve's own "unknown" category, T26), free_to_play (bool default false, T37), shared (bool default false, Steam-only family-sharing flag, T60, mirrors free_to_play)
- db: `playtime_snapshots` — id, owned_game_id, playtime_minutes, captured_at. Appended per sync.
- db: `user_game_meta` — id, user_id, game_id, status enum(unplayed|playing|finished|abandoned), tags[], notes, rating, hidden boolean default false (T25)
- db: `wishlist_items` — id, user_id, game_id, added_at, origin enum(local|steam|gog), steam_present, gog_present, steam_appid (nullable, T49 — raw Steam id for resolution-independent absence keying, mirrors gog_product_id, V49), gog_product_id (nullable), suppressed_at (nullable tombstone), synced_at. unique(user_id, game_id). Added T17, sync cols T20, steam_appid T49.

## §V invariants

V1: `owned_games` ? multiple rows per (user, game_id) — 1 per platform. ⊥ collapse at ingestion (loses launch-platform info). Dedupe at query time | materialized view.
V2: `platform_connections.auth_token` ! encrypted at rest.
V3: ∀ API responses → JSON. ⊥ server-rendered HTML, ⊥ Nuxt-specific payload assumptions.
V4: IGDB mapping cached → repeat sync ⊥ re-query IGDB for known platform_game_id.
V5: Steam auto-poll ≤ 1/day/user + manual refresh. Conservative — limits undocumented.
V6: `user_game_meta` decoupled from platform data → re-sync ⊥ touch user tags/status/notes/rating.
V7: `games` canonical — igdb_id unique, 1 row per real-world game.
V8: sync work async via queue. API dispatches job → returns. ⊥ inline sync in request cycle.
V9: ∀ platform_connections → sync status visible (last_synced_at, in-progress, error).
V10: sync upsert keyed (platform_connection_id, platform_game_id) unique index → re-sync ⊥ duplicate owned_games rows.
V11: IGDB match fail → game still visible in library (`games` row, igdb_id null, provisional). Sync ⊥ silently drop games.
V12: playtime_minutes null = unknown ≠ 0. "Unplayed" = playtime 0 | user status unplayed. Null ∉ unplayed auto-count.
V13: disconnect → soft-keep. owned_games rows persist, connection status disconnected, UI badge. Reconnect restores. ⊥ delete on disconnect.
V14: GOG token refresh before expiry via refresh_token. V2 encryption applies to refresh_token.
V15: Steam private profile → distinct connection error state + user messaging. ⊥ silent 0-game sync.
V16: ∀ sync → append `playtime_snapshots` row per owned_game with playtime data. Burn-down reads snapshots.
V17: email | password change ! verify current_password. ⊥ silent account takeover via stolen bearer token.
V18: API token plaintext → response once @ creation only. Stored hashed (Sanctum), ⊥ retrievable later.
V19: manual adds → per-user synthetic `manual` platform_connection (no tokens, ⊥ sync jobs). V10 upsert key applies → ⊥ duplicate manual rows per game. Manual entries deletable; synced entries ⊥ manual delete.
V20: ∀ discovery responses → `in_library` computed vs caller's owned igdb_ids. ⊥ stale per-user cache: IGDB payloads cacheable globally, ownership overlay per-request. `in_wishlist` same rule (T17).
V21: wishlist ∩ owned = ∅ @ data layer (⊥ wishlist_items row for owned game). Add-to-wishlist of owned game → 200 no-op w/ in_library flag. Promote to library → wishlist row removed. Wishlist ⊥ counted in stats, backlog, system collections. Amended T38 (was "⊥ counted in library"): wishlist rows now ∈ `/api/library` union view w/ library_status=wishlist (V42) — exclusion narrowed to stats layer.
V22: wishlist sync idempotent + asymmetric. Remote removal propagates: item previously platform-present, now absent from pull → local wish deleted (unless other platform still lists | tombstone pending — then flag clears only). ⊥ re-push after remote removal (thrash). Pull steam+gog → upsert (⊥ dups per unique key); local delete → tombstone (`suppressed_at`) → ⊥ re-import while platform still lists it, gog_present rows also pushed remove to GOG; local adds push to GOG only when external mapping resolves; Steam ⊥ pushed ever (no write API). Remote GOG write ≤ 1 per state change. Sync ⊥ run inline (V8 queue).
V23: SUPERSEDED by V41 (T37, amend 2026-07-15) — was: Steam sync ⊥ pass `include_played_free_games`, excludes F2P titles "anyone technically owns" (B3). Now F2P re-included w/ playtime > 0 guard. Manual F2P add (T14) unaffected throughout.
V24: Steam sync reflects current Steam state, ⊥ just accretes — owned_games rows w/ platform_game_id ∉ fresh fetch pruned each sync (covers legacy noise + genuinely-removed games). Snapshot history cascades w/ row (V16 data meaningless off-library). V15 null-response (private) short-circuits before reconciliation → never wrongly wipes on privacy flip.
V25: Steam connect ! resolve+display identity (persona_name/avatar) → user ! confirm before `platform_connections` row created. ⊥ blind-linked accounts.
V26: GameMatcher single-title failure (network/auth/rate-limit) ⊥ abort rest of batch → try/catch around searchGame, ⊥ MISS cache write on exception (retried next sync, ≠ genuine no-match). Mirrors existing timeToBeat tolerance.
V27: GameMatcher retries search once w/ trademark glyphs (®™©) stripped when raw title misses — Steam titles carry glyphs IGDB canonical titles ⊥. Retry only fires when stripping changes query (⊥ extra IGDB call on genuine misses: non-game software, edition-suffix mismatches stay correctly unmatched).
V28: hidden (`user_game_meta.hidden=true`) games ⊥ counted anywhere by default — excluded from `/api/library` (⊥ `include_hidden=1`), system smart collections (unplayed/abandoned/quick_wins), `/api/stats/backlog`. Mirrors V21 (wishlist exclusion) pattern but toggle-based, ⊥ separate table. ∀ new library-aggregating endpoint ! apply same exclusion.
V29: manual collections (type=manual) membership explicit via `collection_games` pivot; filter collections (type=filter) evaluate `filters` JSON at read time (existing T7 behavior). ⊥ mixing — manual collection ⊥ has `filters`, filter collection ⊥ has membership rows. `POST/DELETE .../games` 422 on type mismatch.
V30: platform taxonomy (themes/keywords/game_modes) sourced IGDB-only — Steam has ⊥ public tags API, ⊥ scraped. Stored on canonical `games` row (mirrors genres), populated wherever genres already populated (GameMatcher canonicalize, GameFromIgdb, manual/wishlist add) — 1 IGDB fields list extension, ⊥ new endpoint (V4 cache/throttle unchanged).
V31: Deck compat fetch is best-effort, mirrors V11/timeToBeat tolerance — failure ⊥ fail sync, `deck_status` just stays null (not "unsupported"). Fetched only for platform=steam owned_games rows (gog/manual ⊥ attempted, stay null). null ≠ Valve's "unknown" category (stored as string) — null means "we never successfully checked."
V32: multiplayer/coop/local_multiplayer/local_coop derived from single unified source (IGDB `multiplayer_modes`, OR'd across ∀ returned rows) — ⊥ split across coarser game_modes[] taxonomy (T22), avoids 2 sources disagreeing. `local_coop` = offlinecoop ∨ lancoop specifically (splitscreen alone ⊥ imply coop — could be local competitive). null = not yet fetched/best-effort miss ≠ false (mirrors V31).
V33: ESRB rating sourced IGDB `age_ratings` filtered to ESRB category only (⊥ PEGI/CERO/other orgs mixed in). Nullable — unrated | non-ESRB-market games stay null, ⊥ "Not Rated" placeholder. Best-effort, fetched once (rating doesn't change post-release, ⊥ refetch).
V34: manual "fix match" (T29) reuses V7 canonical-row dedupe exactly (mirrors GameMatcher::canonicalize). Amended 2026-07-15 → T47 (was owned_games-only, B13): operates on whole library union (owned_games ∪ wishlist_items ∪ meta-orphans, mirrors V42) — membership check = union ≠ ownedGames-only. Repoints whichever caller reference rows exist for the game: `owned_games.game_id` (all — V1 multi-platform), `wishlist_items.game_id`, `user_game_meta.game_id` → chosen canonical row (existing | newly created). Unique-per-user rows (wishlist, meta — unique(user_id,game_id)) merge: if caller already has a target-game row, drop the source row ≠ collide. Meta follows the game (preserves V6 status/tags/notes/rating content, just re-attached) — never force-dropped. Orphaned provisional `games` row deleted ONLY when 0 refs remain across ALL owned_games + wishlist_items + user_game_meta (any user) — row still owned/wishlisted/meta'd by anyone = real data, kept (extends old "unowned provisional" cleanup). V21 guard: repointing a wishlist row onto an igdb_id the caller already owns ⊥ create owned∩wishlist overlap → drop that wishlist row (owned wins, V21 promote rule). Available regardless of current match state (provisional | already-matched) — corrects wrong auto-match, ⊥ just fill misses.
V35: manual per-game IGDB refresh (T30) requires an existing igdb_id — 422 else, direct user to fix-match first. Synchronous (mirrors T14's inline `GameFromIgdb` call), ⊥ queued — V8 scopes bulk connection sync only, not a bounded single-record on-demand fetch.
V36: `GET /api/library/facets` (T28) returns the full library vocabulary, unfiltered by any other active filter selection on the same request — v1 static facets, ⊥ narrowing/responsive faceted search. Hidden-game exclusion is V28 applied here (new library-aggregating endpoint), ⊥ a separate rule.
V37: IGDB `age_ratings` real shape (live-verified, B7) — flat `category`+`rating` fields T27 assumed ⊥ exist (IGDB overhaul, pre-2026); actual: `organization` int (→ `age_rating_organizations`, ESRB=1) + `rating_category` ref, nested-expandable to `.rating` (human label string directly — "M","E10+" etc, ⊥ local numeric id→label map). Unknown/renamed fields IGDB silently drops (bare `{id}` row, no error) rather than failing the request — a wrong field name here fails silent, never loud. Query `age_ratings.organization,age_ratings.rating_category.rating` — still 1 request, V4 throttle/cache unaffected.
V38: bulk "sync all IGDB" (T31) queued (extends V8's rationale — bulk work off request cycle — beyond connection-sync scope). Throttled per-user ≥5 min gap, Redis cache key (mirrors connection sync-now throttle, §C), ⊥ new DB column. Runs `GameMatcher::matchConnection` per caller connection (provisional games, V26 tolerance already covers per-game failure) + `GameIgdbRefresh::refresh` per already-matched game — 1 game's failure ⊥ abort the batch, mirrors V26. Fan-out per V39 (B8) — was single monolithic job, timed out.
V39: bulk IGDB work fans out — orchestrator job ⊥ makes IGDB calls itself, only dispatches child jobs: 1 per connection match unit + 1 per game refresh. Child job runtime bounded (≤ few IGDB calls). Rate limit enforced @ queue layer (job middleware, Redis throttle, shares ≤ 4 req/s budget V4/§C discovery) → over-budget child released back to queue w/ delay, ⊥ sleep-blocking worker. Child failure isolated per V26/V38 — 1 game fail ⊥ touch siblings. Retry mid-batch resumes remaining children, ⊥ restart whole library.
V40: filter sidebar 1 control per concept. Amended 2026-07-15 → T41 (reverses T34 resolution): game-mode FacetFilter = the single control — standalone Multiplayer/Co-op/Local multiplayer/Local co-op checkboxes removed. Combobox options = facets.game_modes full (⊥ exclusion, drop `excludeDedicatedGameModes`) ∪ synthetic "Local multiplayer","Local co-op" (⊥ IGDB game_modes value exists for those). Selection mapping: bool-backed labels → V32 bool params ("Multiplayer"→multiplayer, "Co-operative"→coop, synthetics→local_multiplayer/local_coop), ⊥ game_mode facet param — V32 flags stay source of truth where both sources cover same concept; residual labels ("Split screen","Single player","Battle Royale"…) → game_mode param. API untouched — params all pre-exist (I.api). Was: exclude duplicated values from list, keep checkboxes (T34). ∀ new facet ! check overlap vs dedicated filters before rendering.
V41: F2P inclusion guard (supersedes V23, keeps B3 noise out): Steam sync fetches owned games 2× (base + `include_played_free_games=1`), appid ∈ extended ∖ base = F2P → `owned_games.free_to_play=true`. F2P row ingested iff playtime > 0 — zero-playtime F2P ⊥ ingested, existing rows pruned via V24 reconciliation (fresh-set = base ∪ played-F2P). Paid rows unaffected (playtime 0|null fine).
V42: library union — `GET /api/library` = owned_games ∪ wishlist_items ∪ meta-orphans (user_game_meta w/o owned/wishlist row, survives V6/V24). Per-game `library_status` precedence: owned (∃ owning row ⊥ free_to_play) > free (∃ owning rows, ∀ free_to_play) > wishlist (wishlist_items row only) > none (meta only). Stats/backlog/system collections count owned+free only — wishlist (V21) + none excluded. V28 hidden-exclusion applies across whole union. Data-layer disjointness (V21, V10 keys) unchanged — union assembled at query time. Amended amend 2026-07-16 → T60 (V58): `shared` tier inserted between free and wishlist — owned (∃ owning row ¬free_to_play∧¬shared) > free (∃ owning row free_to_play, ⊥ plain-owned) > shared (∃ owning row shared, ⊥ owned/free) > wishlist > none. Stats/backlog/system-collection inclusion set (was owned+free) extended owned+free+shared (V58 — shared fully playable).
V43: `games.esrb_rating` = IGDB label string verbatim ("E10+","M") — opaque, ⊥ hardcoded label enum in query/facet/FE layer. `esrb[]` validated free string; unknown label → 200 ∅, ⊥ 422. ∀ facets-emitted value ! round-trip: accepted by matching filter param + filters by equality vs stored. `none` = only synthetic value (query layer only, V33). Test = facet→filter round-trip.
V44: sidebar↔collection round-trip (extends V43 class): ∀ filter param FE sidebar emits ! ∈ `ALLOWED_FILTER_KEYS` — current sidebar state saveable as type=filter collection verbatim, ⊥ silent key drop. Saved filters JSON evaluates via same LibraryQuery path as direct query params (V29) → saved collection ≡ re-checking same facets. ∀ new sidebar filter param ! added to whitelist + round-trip test.
V45: IGDB `external_games` mapping (gameIdFromExternal/externalUid, wishlist sync T20) filtered by `external_game_source` — ⊥ deprecated `category`. EXTERNAL_STEAM=1/EXTERNAL_GOG=5 = external_game_source ids (live-verified, unchanged from old category values). Wrong field → IGDB 200 `[]` silent (never errors) → every mapping misses → wishlist imports all provisional (no cover/genres). Same failure class V37/B7 + V43/B10: ∀ IGDB field name ! live-verified, silent-empty ≠ genuine no-match. Test = external mapping query emits `external_game_source`.
V49: Steam wishlist absence reconciliation keyed on raw appids (mirrors GOG raw productIds, V22) — `wishlist_items.steam_appid` stored @ pull (recordPresence), `$gone` = steam_present rows whose steam_appid ∉ fresh appid set. Resolution-independent → transient/unresolved item stays (its appid ∈ set), never reaped; also retains delisted-but-present appid (caveat1). Subsumes V47: incomplete-pull guard becomes unnecessary (reconciliation safe even mid-failure) → V47 retired when T49 ships (until then V47 active). Backfill: existing rows steam_appid NULL → SQL `NULL NOT IN (...)` = NULL ⊥ matched ∴ ⊥ in `$gone` (kept until 1st post-T49 sync populates the column). Orphan cleanup: when a heal/absence delete drops a provisional-backed wishlist row, delete the orphaned provisional `games` row iff 0 refs (reuse GameRematch::referencedAnywhere, V34) — caveat2.
V48: bulk "sync all IGDB" (T31/T32, SyncLibraryIgdb) operates on whole library union — owned ∪ wishlist_items ∪ meta-orphans (V42), ⊥ owned-only. Both halves: refresh dispatched per matched game across union (⊥ just ownedGames() join), match dispatched per provisional game across union (owned via connection + wishlist-only + meta-orphan). Provisional wishlist/meta match reuses union-aware repoint (GameRematch/V34) — canonicalize ! repoint wishlist_items + user_game_meta, ⊥ owned_games only. Fan-out/throttle unchanged (V38/V39). Same class as B13 (rematch owned-only) + V28/V42: ∀ library-wide IGDB op ! cover the union, ⊥ ownedGames() scope.
V50: platform sync (SyncConnection, initial + subsequent) auto-queues IGDB sync for ∀ owned game whose canonical `games.igdb_synced_at` is null | >24h stale — provisional → match, matched → refresh. Freshness stamped `igdb_synced_at = now()` @ every successful IGDB canonical write: match (GameMatcher::canonicalize), create (GameFromIgdb::findOrCreate), refresh (GameIgdbRefresh). Fresh (<24h) games skipped → frequent re-sync (V5 daily + "sync now" ≥5 min) ⊥ redundant IGDB volume. Fanned out per V39 (rate-limited child jobs — MatchConnectionIgdb provisional half + RefreshGameIgdb stale-matched half, scoped to the connection's owned games), ⊥ inline IGDB loop in the sync job (extends V8/B8 — refreshing ∀ matched game every sync serial-inline would re-blow the queue timeout). Per-game failure isolated (V26). Provisional half stays connection-scoped (V4 platform_game_id cache + 24h MISS TTL already throttle re-query) — 24h gate governs the refresh half. Was: SyncConnection matched provisional-only inline, matched games never refreshed on sync.
V47: Steam wishlist absence reconciliation ⊥ run on incomplete pull. Transient per-item resolution failure (caught IGDB/Twitch/Steam-store exception, V46) → an appid still on remote wishlist may be missing from `$seen`; reaping against that partial set wrongly deletes present items (B14). ∴ any transient failure during pullSteam → skip handleRemoteAbsence this sync (genuine removals propagate next fully-resolved sync). Distinct from clean no-resolution (delisted appid, appName null → no throw) which stays pre-T46 reconcile behavior. GOG exempt — keys absence on raw `productIds` (V22), resolution-independent. ∀ absence reconciliation keyed on RESOLVED ids (⊥ raw remote ids) ! guard vs partial pulls.
V46: wishlist external-id resolution (gameFromExternal: IGDB external mapping + Steam store-title fallback) best-effort per-item — V26 class. One item's IGDB/Twitch/Steam-store failure skips that item (unresolved, retried next sync), ⊥ aborts sync batch. Exception ⊥ written to negative cache (mirror V26 MISS-on-exception); genuine no-mapping ⊥ cached forever (short TTL | recheck) so a later IGDB mapping backfills. Steam store 500s = rate-limit noise, tolerated like deck compat (V31).
V51: wishlist ⊥ standalone page/route. Wishlist rows surfaced ONLY via library union (V42/T38 — `library_status=wishlist` filter + GameCard badge); promote-to-owned + per-item actions live on GameCard/game-detail. Platform wishlist sync (POST /api/wishlist/sync, T20) control+status lives on /profile connected-services (T13), ⊥ separate wishlist UI. Redundant `/wishlist` page removed T52. ∀ future wishlist UI reuses union surface + /profile — ⊥ new dedicated route.
V52: ∀ queued job w/ RateLimited middleware ! bound retries by TIME (retryUntil), ⊥ attempt count. A limiter release (`$job->release`) counts as an attempt → a count ceiling (default tries=1) fails flooded jobs w/ MaxAttemptsExceededException BEFORE handle() runs (B16). igdb-sync fan-out (MatchGameIgdb/RefreshGameIgdb/MatchConnectionIgdb, perSecond(2) global) → shared `RateLimitedIgdbSync` trait: retryUntil = now+15min (drains 1 user's union backlog @ 2/sec) + maxExceptions=3 (cap genuine error loop, ⊥ retry till deadline). Test = fan-out job retryUntil future + maxExceptions set. Same class extends V39 (rate-limit @ queue layer) — release semantics ⊥ free.
V53: playtime stat ⊥ shown for unowned entries (library_status ∈ wishlist|none). API already nulls playtime for these rows (§I `GET /api/library`), but `formatPlaytime(null)` → literal "Playtime unknown" string, rendered unconditionally (GameCard.vue, games/[id].vue) — misleading, implies ownership w/ missing data rather than "concept doesn't apply". Fix = gate playtime row on library_status ∈ owned|free (V42 precedence — free = owning row, playtime legitimately applies, V41), hide entirely for wishlist|none, ⊥ change formatPlaytime itself (V12 null-vs-zero semantics still correct for owned rows). Same class as T54 (V42 field exists in API, FE surface gap). Amended amend 2026-07-16 → T60 (V58): eligible set extended owned|free|shared — shared rows always have playtime_minutes null (V58, untracked-by-design, ⊥ "not yet acquired" like wishlist/none), so this renders "Playtime unknown" same as a null owned row (V12 semantics: null = legitimately unknown, not inapplicable). Hidden set stays wishlist|none only.
V54: ∀ public OAuth client id consumed client-side ! (a) documented in a checked-in .env.example for that app (mirrors api/.env.example pattern — client ids are public, safe to name the key even though value stays unset), (b) guarded before building an external redirect URL — empty/missing id fails loud w/ local inline message, ⊥ silently building a broken URL and letting the external IdP's error be the user's first signal. Extends V15 (Steam private-profile distinct error state) to the OAuth-config-missing case.
V55: ∀ manual-paste OAuth code flow (no redirect-URI control, T6-class) ! accept the full pasted redirect URL, ⊥ require the user to hand-extract just the code substring. GOG's `code` param sits inside `?origin=client&code=...&session_id=...` on the landing page — manual substring extraction is error-prone (stray params, whole-URL pastes, un-decoded `%XX` sequences) and every variant gets rejected identically by the token endpoint's opaque "code rejected" response. Parse `code` out of whatever's pasted (URLSearchParams, handles both full URL and bare query string), fall back to the trimmed raw value so a clean bare-code paste still works unchanged.
V56: ∀ external OAuth/API rejection (non-2xx from a 3rd-party token/auth endpoint) ! log the actual upstream error body server-side, ⊥ swallow to a generic null/bool + one blanket user-facing message. `invalid_client` (bad credentials) and `invalid_grant` (bad/expired code) are different failure classes needing different fixes — collapsing both to "GOG rejected that code" cost a full guess-and-check round-trip (B19) that a single log line would've skipped. Extends V26 (per-item failure isolation) — isolation ⊥ excuse to discard the reason. Applies wherever a 3rd-party auth call returns null-on-failure (GogClient::requestToken; same shape as IGDB/Twitch calls, V26 class).
V57: personal rating (`user_game_meta.rating`) ⊥ shown for library_status ∈ wishlist|none — same class as V53 (playtime hide). `PUT .../meta {rating}` unaffected (V6 meta stays settable regardless of ownership) — display-only gate on GameCard + game detail rating stars, eligible set = owned|free|shared (V58), mirrors T55 pattern. Added amend 2026-07-16 → T59.
V58: Steam Family Sharing → library_status=shared. Research spike 2026-07-16 ruled out the `IFamilyGroupsService/GetSharedLibraryApps` route — it needs a live Steam web-session `access_token`, not an API key; storing that means storing the user's actual Steam login session, a heavier trust model than any existing connection (rejected). Viable mechanism instead, public APIs only: user manually adds family members by SteamID/vanity (resolve+confirm mirrors V25) → synthetic `platform_connections` row per member (platform=steam_family, no tokens, mirrors `manual` synthetic pattern V19) → sync (reuses existing `POST /api/connections/:id/sync`, SyncConnection branches on platform) fetches that member's `GetOwnedGames` (public), keeps appids whose `appdetails.categories` include id=62 ("Family Sharing", public/unauthenticated, best-effort — ⊥ guaranteed live entitlement, post-purchase revocation not exposed) and that aren't already in caller's own owned_games (caller's real ownership always wins) → upserts `owned_games` rows on that synthetic connection w/ `shared=true`, **`playtime_minutes` forced null** (fetched playtime belongs to the family member's account, not caller's — showing it would misattribute usage, V12-class distinction). Reconciliation mirrors V24 (prune shared rows whose appid drops out of that member's fresh category-62-filtered fetch), scoped to that one synthetic connection; family member's profile going private short-circuits before pruning (V15-style, avoids wrongly wiping on transient privacy flip). Removing a family member hard-deletes their `family_members` row + synthetic connection + all `shared` rows sourced from it (⊥ V13 soft-keep — no real "your data" to preserve). V42 tier unchanged: owned (∃ row ¬free∧¬shared) > free (∃ row free_to_play, none plain-owned) > shared (∃ row shared, none owned/free) > wishlist > none. Stats/backlog/system collections count shared same as owned+free (fully playable). Added amend 2026-07-16 → T60; revised same day — originally drafted around the undocumented-session mechanism, replaced after research spike.
V59: rebrand/copy-audit tasks ! grep for retired-name METAPHOR words, ⊥ just the literal old brand string. T42 swapped `GameShelf`→`GameBower` literal occurrences only (header logo, hero body sentence, footer) — missed leftover "shelf" product-metaphor copy woven into welcome.vue (step title, hero span, CTA) that names the old concept w/o spelling the old brand. Same failure shape as B7/B10/B11 (assumed-scope silently incomplete, no error, just wrong). Checklist: (a) literal old name string, (b) any imagery/metaphor language derived from it, both scoped to ∀ user-facing copy, ⊥ just the marketing page. Added amend 2026-07-16 → T61 (B20).
V60: ∀ platform integration (existing | future) ! authenticate via real OAuth (user consents at the provider's own login) | a public unauthenticated API — ⊥ password-equivalent session tokens (e.g. PSN NPSSO) | scraped session cookies/stored credentials (e.g. Humble Bundle), regardless of whether a community library exists for it. Formalizes the ad hoc reasoning V58 used to reject Steam family sharing's session-token route into a standing policy for ∀ platform, ⊥ just Steam. PlayStation + Humble Bundle evaluated 2026-07-16, rejected on this basis (§C). Xbox accepted — real Microsoft OAuth → XSTS token exchange is the same trust class as existing Steam OpenID/GOG OAuth, ⊥ a session-token route. Added amend 2026-07-16 → T63.

## §T tasks

id|status|task|cites
T1|x|Laravel API skeleton + Sanctum auth + email/password accounts|V3
T2|x|Nuxt skeleton + auth flow → Sanctum|V3
T3|x|Steam connect (vanity resolve) + queued sync job + raw ingestion + snapshots|V2,V5,V8,V9,V10,V15,V16,V23,V24,I.steam
T4|x|canonical game matching vs IGDB (Twitch auth), Redis cache, provisional fallback|V4,V7,V11,V26,V27,I.igdb
T5|x|unified library UI: grid, filters, sort, disconnect badges|V1,V12,V13,I.api
T6|x|GOG connect + token refresh + sync — validates multi-platform dedupe|V1,V2,V8,V10,V14,I.gog
T7|x|tags/status/smart collections (quick wins conditional)|V6,V12,I.api
T8|x|backlog stats view + shareable card — burn-down from snapshots|V16,I.api
T9|.|(stretch) iOS client vs existing Sanctum API|V3
T10|x|migrate web/ Nuxt 3 → 4: bump nuxt dep, `app/` srcDir (already set), compat fixes; keep `ssr: false`; auth flow + library UI ⊥ regress|V3,§C.nuxt-mode
T11|x|adopt Tailwind CSS in web/; restyle ∀ views dark slate + teal (login, register, library grid, GameCard, badges)|§C.styling,§C.theme
T12|x|landing marketing page `/welcome`: hero, feature pitch (connect→dedupe→triage backlog), CTA → register/login; public route in auth.global; guests hitting `/` → `/welcome`|§C.landing,§C.theme
T13|x|profile page `/profile`: account section (email/password change vs current_password), connected services (list + connect steam/gog + sync now + disconnect vs existing I.api), API keys (list/create w/ once-shown plaintext/revoke). Connected-services also hosts "sync all IGDB" (T31) + "sync wishlist" (T52, relocated from removed wishlist page, V51)|V2,V13,V17,V18,I.api,§C.theme
T14|x|manual add: `manual` platform enum + synthetic connection, POST /api/library {igdb_id}, manual delete, library UI add/remove affordances|V1,V7,V10,V19,I.api
T15|x|discover: /api/discover search+browse (IGDB proxy, Redis cache, in_library overlay), FE /discover page — search bar, browse grid w/ genre/sort, add-to-library button|V4,V19,V20,I.igdb,§C.discovery,§C.theme
T16|x|similar-games rails: seeds from top owned, IGDB similar_games cached, "Because you played X" sections on /discover|V4,V20,§C.discovery
T17|x|wishlist core: wishlist_items table, GET/POST/DELETE /api/wishlist (games created from igdb_id via shared GameFromIgdb service), in_wishlist overlay, wishlist page + promote-to-owned flow. Wishlist page removed T52 (redundant vs library union V42/T38); promote-to-owned relocated → GameCard/detail (V51)|V7,V20,V21,I.api
T18|x|franchise gaps: IGDB franchise lookup for owned games, /api/discover/franchises, "complete the series" rail|V4,V20,§C.discovery
T19|x|upcoming releases: IGDB release_dates ∈ 6 mo window × caller top genres, /api/discover/upcoming, rail on /discover|V4,V20,§C.discovery
T20|x|wishlist platform sync: queued job — pull steam (read-only, appdetails titles) + gog wishlists, tombstone suppression, push local add/remove → GOG via external_games mapping, POST /api/wishlist/sync throttled, sync status in wishlist UI. Sync control+status → /profile connected-services T52 (was wishlist page, removed V51)|V8,V14,V15,V21,V22,I.gog,I.steam,I.igdb
T21|x|Steam connect identity confirm: resolve+preview (persona_name/avatar) before creating connection, FE two-step confirm|V25,I.steam
T22|x|advanced filtering: pull IGDB themes/keywords/game_modes onto `games` (extend IgdbGameAttributes + IgdbClient field lists, populate at match/create time), extend GET /api/library filter vocab (theme/keyword/game_mode params), FE filter UI. User freeform tags (T7) unaffected|V4,V30,I.api
T23|x|manual collections: `collections.type` enum(filter\|manual), `collection_games` pivot, POST/DELETE /api/collections/:id/games, FE add-to-collection / remove-from-collection affordance|V29,I.api
T24|x|game detail view: GET /api/library/:game_id show route, FE /games/:game_id page — cover/genres/platforms (+ deck_status badge per platform, T26)/time_to_beat/themes/keywords/game_modes/esrb_rating/multiplayer+coop+local badges (T27) + editable status/tags/rating/notes via existing PUT meta. library_status badge added T54|I.api
T25|x|show/hide: `user_game_meta.hidden` bool, PUT meta `hidden?`, GET /api/library exclude-by-default + `include_hidden=1` toggle, exclude hidden from smart collections + backlog stats, FE hide/unhide button + reveal-hidden toggle|V28,I.api
T26|x|Steam Deck status: `owned_games.deck_status` nullable enum, fetch via undocumented saleaction/ajaxgetdeckappcompatibilityreport per Steam owned_game every sync (best-effort, V31), GET /api/library platforms[] carries deck_status + `deck_status[]` multi-select filter, FE badge on GameCard + detail view (T24) + filter UI|V31,I.steam
T27|x|ESRB + multiplayer flags: IGDB `age_ratings` (ESRB category) → `games.esrb_rating`, IGDB `multiplayer_modes` → `games.multiplayer/coop/local_multiplayer/local_coop` (unified source, V32), both best-effort @ match/create time (mirrors time_to_beat, T7); GET /api/library filter vocab extended (esrb, multiplayer, coop, local_multiplayer, local_coop), FE filter UI + detail view (T24) badges|V32,V33,V37,I.igdb
T28|x|library search + left-sidebar checkbox filters: GET /api/library `q` param (title substring) + GET /api/library/facets (distinct genre/theme/keyword/game_mode/platform values in caller's library); FE reflow filter panel to left sidebar — genre/theme/keyword/game_mode/platform become checkbox lists sourced from facets, text search box at top of grid|V28,V36,I.api
T29|x|manual "fix match": POST /api/library/:game_id/rematch, query-override candidate search reuses GET /api/discover/search; FE fix-match flow on detail page — override search term, results grid, select to apply|V7,V34,I.api,I.igdb
T30|x|manual per-game IGDB refresh: POST /api/library/:game_id/refresh-igdb; FE "fetch" button on detail page (T24) to re-sync one game's IGDB data on demand|V35,I.api,I.igdb
T31|x|"sync all IGDB" bulk refresh: POST /api/library/sync-igdb → 202 dispatch job (throttled per-user ≥5 min), job runs GameMatcher::matchConnection per caller connection (provisional games) + GameIgdbRefresh::refresh per already-matched game (best-effort, ⊥ abort batch on single failure); FE button on /profile|V38,I.api
T32|x|fix T31 timeout: SyncLibraryIgdb → fan-out orchestrator, new per-game RefreshGameIgdb job + per-connection match job, RateLimited/Redis-throttle middleware on IGDB budget|V38,V39,I.api
T33|x|facet combobox: add `@headlessui/vue`, FacetCombobox component (multi-select, searchable), LibraryFilterSidebar renders combobox when facet > 5 options else checkbox list; re-enable commented-out keywords facet via combobox|§C.facet-combobox,I.api
T34|x|dedupe multiplayer/co-op options: exclude game_modes values duplicating V32 bool filters from sidebar game-mode list — resolution reversed by T41|V40,V32
T35|x|∀ facets → combobox: FacetFilter always renders FacetCombobox — drop `facetControlKind` threshold + checkbox branch + threshold tests; sidebar untouched (∀ 5 facets already route FacetFilter)|§C.facet-combobox
T36|x|ESRB + Deck status → FacetFilter: API `esrb` single → `esrb[]` multi (mirrors `deck_status[]`); facets endpoint + `LibraryFacets` gain `esrb_ratings[]` (distinct in-library values, V28 hidden-exclusion + V36 static-vocab apply); FE sidebar replaces ESRB select + Steam Deck checkbox fieldset w/ FacetFilter — ESRB options ← facets, deck status static list; `none` sentinel: facets emit when ∃ null-rated games, `esrb[]=none` filters `esrb_rating` IS NULL, FE labels "No Rating" — storage stays null (V33), sentinel = query/facet layer only|§C.facet-combobox,I.api,V28,V33,V36
T37|x|F2P re-inclusion: migration `owned_games.free_to_play` bool default false; SteamClient double-fetch (base + `include_played_free_games=1`), diff marks F2P; SteamSyncer ingests F2P iff playtime > 0, V24 fresh-set = base ∪ played-F2P|V41,V24,V10,I.steam
T38|x|library union + Library Status filter: GET /api/library unions wishlist_items + meta-orphans w/ owned rows, `library_status` per entry (precedence V42), `library_status[]` param; stats/backlog/system collections owned+free only; FE sidebar FacetFilter (static owned\|free\|wishlist\|none) + wishlist badge on GameCard|V42,V21,V28,I.api
T39|x|interactive star rating on library view: GameCard 5 inline star emojis (★ filled/☆ hollow from meta.rating), click star n → PUT /api/library/:game_id/meta {rating: n}, click current rating → clear (null)|V6,I.api
T40|x|personal rating facet: `rating[]` param (1\|2\|3\|4\|5\|none — none = rating IS NULL); FE sidebar FacetFilter, static options w/ star labels + "Unrated"|I.api,V28
T41|x|unify game-mode filter (reverses T34): drop `excludeDedicatedGameModes` exclusion, remove standalone Multiplayer/Co-op/Local multiplayer/Local co-op checkboxes; game-mode combobox options = facets.game_modes ∪ synthetic "Local multiplayer","Local co-op"; selection maps bool-backed labels → V32 bool params, rest → game_modes[] param|V40,V32,I.api
T42|x|rebrand GameShelf → GameBower: FE logo spans (index/login/register/welcome/stats "Game<span>Shelf"), welcome.vue copy ×3, authState keys `gameshelf_token`/`gameshelf_auth_user`/`gameshelf_auth_token` → `gamebower_*` (+ useAuth.spec expectations), api/.env APP_NAME=GameBower; local APP_URL/Herd site unchanged (§C.brand)|§C.brand,§C.theme
T43|x|adopt Nest Hex design system (after T42): Tailwind theme tokens (bg `#171A21`, slate `#3A4556`, indigo `#1A1E4A`, teal `#2FA7A0`, violet `#7D6BFF`) → restyle base bg/surfaces/accents vs current generic slate-teal; `BrandWordmark` component ("Game"+"Bower", teal "w") replaces 5 inline logo spans; `BrandMark` inline-SVG nest-hex (nested hexagons + teal alcove) in header + welcome hero; favicon (circle brandmark on dark)|§C.brand,§C.theme
T44|x|on-the-fly smart collection from checked facets: extend `ALLOWED_FILTER_KEYS` + deck_status, esrb, multiplayer, coop, local_multiplayer, local_coop, q (V44 round-trip test); FE library sidebar "Save as collection" affordance (visible when ≥1 filter active) → name prompt → POST /api/collections {name, type: filter, filters: current sidebar state}; FE collection picker on library view (system + custom) → sets existing `collection` param — first FE surface for filter collections|V44,V29,I.api
T45|x|fix wishlist external mapping: IgdbClient gameIdFromExternal + externalUid query `external_game_source` not `category` (B11/V45); fix EXTERNAL_STEAM/EXTERNAL_GOG comment; flush poisoned `igdb-external:*` cache (0-cached forever from prior misses) → existing provisional steam-origin wishlist rows self-heal on next sync via V24-style absence reconciliation|V45,I.igdb
T46|x|harden wishlist sync: try/catch per item in WishlistSyncService::gameFromExternal (IGDB mapping + appName fallback) → skip failed item ⊥ abort batch (V46); stop caching negative external lookups forever (TTL | skip-on-null) so B11-fixed mappings backfill|V46,I.igdb
T47|x|fix rematch on wishlist/union games (B13): RematchController membership check owned→union (owned_games ∪ wishlist_items ∪ meta-orphan user_game_meta); GameRematch::apply repoint owned_games.game_id + wishlist_items.game_id + user_game_meta.game_id for caller (unique rows merge-drop on target collision; meta follows game per V6), V21-guard wishlist repoint onto already-owned igdb_id (drop wishlist row), orphan-cleanup delete provisional only when 0 owned+0 wishlist+0 meta refs remain; tests: rematch wishlist-only game, rematch meta-orphan, V21 overlap, orphan-kept-when-still-wishlisted|V34,V42,V21,I.api
T48|x|fix Steam wishlist transient-failure data loss (B14/V47): move per-item try/catch from gameFromExternal into pull loops; pullSteam tracks transient failure → skip handleRemoteAbsence when pull incomplete; pullGog unaffected (raw-id keyed, V22); tests: transient 500 keeps still-present row, clean no-resolution still reconciles|V47,V46,V22
T49|x|Steam wishlist raw-appid absence keying + provisional-heal orphan cleanup (caveat1/caveat2, V49): migration wishlist_items.steam_appid nullable; pullSteam stores appid @ recordPresence, `$gone` keyed on steam_appid ∉ fresh appids (mirror GOG/V22) → retires V47 incomplete-pull guard (delete the `$incompletePull` skip, reconciliation now resolution-independent); delisted-but-present appid retained; on heal/absence delete of a provisional-backed wishlist row delete orphaned provisional game iff 0 refs (reuse GameRematch::referencedAnywhere); NULL-backfill safe (null steam_appid ⊥ reaped until populated); tests: transient failure keeps row AND still reconciles genuine removal, delisted appid retained, orphan provisional cleaned, backfill-null row kept|V49,V47,V22,V34
T50|x|bulk sync-all-IGDB covers library union (B15/V48): SyncLibraryIgdb refresh half enumerates matched games across owned ∪ wishlist_items ∪ meta-orphan user_game_meta (⊥ ownedGames() only); match half dispatches per provisional wishlist/meta game too — new per-game match child job (GameMatcher title search + GameRematch union-repoint V34), ⊥ connection-scoped only; fan-out/throttle per V38/V39; tests: provisional wishlist game matched by bulk sync, matched wishlist game refreshed, meta-orphan provisional matched|V48,V38,V39,V34,V42,I.api
T51|x|auto-queue IGDB sync on platform sync (V50): migration `games.igdb_synced_at` nullable timestamp; stamp `igdb_synced_at = now()` in GameMatcher::canonicalize + GameFromIgdb::findOrCreate (create path) + GameIgdbRefresh::refresh; SyncConnection replaces inline `matchConnection` w/ fan-out — MatchConnectionIgdb::dispatch (provisional half) + RefreshGameIgdb::dispatch per owned matched game IGDB-stale >24h (igdb_synced_at null | < now-24h), skip fresh; scope = connection's owned games; fan-out/throttle V39; tests: stale matched owned game refresh-queued, fresh (<24h) matched game skipped, provisional → MatchConnectionIgdb, igdb_synced_at stamped on match+create+refresh|V50,V39,V26,V8,V5
T52|x|remove standalone wishlist page + move sync → profile (V51): delete `/wishlist` page/route + nav link (redundant — wishlist rows already surface in library union via `library_status=wishlist` filter + GameCard badge, T38/V42); relocate "sync wishlist" button + sync status (POST /api/wishlist/sync, T20) → /profile connected-services section (beside sync-now + sync-all-IGDB, T13/T31); promote-to-owned flow → GameCard/game-detail (POST /api/library, ⊥ wishlist page); tests: `/wishlist` route absent (404/redirect), /profile renders wishlist-sync control+status, promote works from card/detail|V51,V21,V42,I.api,§C.theme
T53|x|fix wishlisted-game resync "too many tries" (B16/V52): extract `App\Jobs\Concerns\RateLimitedIgdbSync` trait (middleware RateLimited('igdb-sync') + retryUntil now+15min + maxExceptions=3), apply to MatchGameIgdb + RefreshGameIgdb + MatchConnectionIgdb (drop their per-class middleware()) → limiter release (=attempt) no longer exhausts default tries=1; time-bounded retries drain the wishlist fan-out backlog @ 2/sec instead of failing; tests: 3 fan-out jobs retryUntil future + maxExceptions=3|V52,V39,V38
T54|x|game detail page shows library status: FE /games/:game_id (T24) renders library_status badge (owned\|free\|wishlist\|none) — API show route already returns it (LibraryQuery, V42), FE-only gap, mirrors GameCard badge (T38); reuse badge component/markup, ⊥ new backend work|V42,T24,T38,I.api
T55|x|hide playtime stat for unowned games (V53): GameCard.vue (line ~57) + games/[id].vue (lines ~139,~155) gate playtime row on `entry.library_status ∈ {owned, free}` — v-if wrap, ⊥ render for wishlist\|none; formatPlaytime util unchanged (V12 null-handling still correct where shown); tests: GameCard hides playtime for wishlist/none entry, shows for owned/free (incl. null="Playtime unknown" case), detail page same|V53,V42,V12
T56|x|fix GOG login missing client id (B17/V54): add web/.env.example (NUXT_PUBLIC_API_BASE, NUXT_PUBLIC_GOG_CLIENT_ID — mirrors api/.env.example); ProfileConnections.vue guards gogAuthUrl — empty config.public.gogClientId → render inline "GOG login unavailable (missing client id)" message instead of dead link; §C env list extended w/ NUXT_PUBLIC_GOG_CLIENT_ID (FE public client id, same value as GOG_CLIENT_ID — public, not secret); tests: gogAuthUrl guard renders fallback message when clientId empty, renders link when present|V54,V15,§C
T57|x|fix GOG login code rejected (B18/V55): `extractGogCode` util (connections.ts) — accepts bare code or full pasted redirect URL, parses `code` query param via URLSearchParams (handles full-URL paste, bare query-string paste, `%XX`-encoded values), falls back to trimmed raw input when no `code` param found; ProfileConnections.vue connectGog() uses it instead of raw `.trim()`; copy updated ("paste the final page's URL (or just the code value)"); tests: extracts from full URL, extracts from bare query string, URL-decodes value, falls back on bare-code paste, empty input|V55,T6
T58|x|fix GOG login credential mismatch + log opaque token-rejection (B19/V56): api/.env GOG_CLIENT_SECRET was 63 chars (truncated, dropped trailing digit) vs GOG's genuine 64-char secret — corrected value, verified live against auth.gog.com/token (bogus-code probe: invalid_client → invalid_grant confirms pairing now valid); GogClient::requestToken logs actual GOG error body (status+error+error_description) on 400/401 instead of silently returning null, ⊥ change to null-return contract (callers unaffected)|V56,T6
T59|x|hide personal rating for unowned-copy entries (V57): GameCard.vue + games/[id].vue gate rating stars/block on `entry.library_status ∈ {owned, free, shared}` — v-if wrap, ⊥ render for wishlist\|none; PUT meta rating unaffected (V6, still settable while wishlisted); tests: GameCard hides rating for wishlist/none entry, shows for owned/free/shared|V57,V58,V42
T60|x|Steam Family Sharing → library_status=shared (V58, mechanism finalized via 2026-07-16 research spike — public APIs only, no session token): migration `family_members` table (id, user_id, steam_id, persona_name, avatar_url, platform_connection_id FK, created_at, unique(user_id,steam_id)); `platform_connections.platform` enum + `steam_family`; migration `owned_games.shared` bool default false (mirrors free_to_play T37); GET/POST/DELETE /api/family-members (resolve+confirm reuses GET /api/connections/steam/resolve, V25); SyncConnection branches on platform=steam_family → family-share ingestion (GetOwnedGames for member's steam_id + appdetails category-62 filter + caller-owned exclusion, forces playtime_minutes null, V24-style reconcile scoped to that connection, V15-style private-profile short-circuit); GET /api/library `library_status` enum gains `shared`, `library_status[]` filter + facets/static FE list gain `shared` option; FE /profile family-members management UI (add via resolve+confirm, list, remove); GameCard/detail badge "Shared" + attribution (family member persona_name via connection join); stats/backlog/system collections count shared same as owned/free; tests: category-62+not-owned game surfaces as shared, own-owned game excludes shared duplicate even if also in member's list, private member profile skips reconcile without wiping, member removal cascades connection+shared rows|V58,V42,V25,V19,V24,V15,V21,I.api,I.ext
T61|x|fix B20: welcome.vue leftover "shelf" metaphor copy (T42 missed it, literal-string-only rebrand) → rewrite off bower/nest imagery (matches Nest Hex identity, T43): step title "One shelf"→"One bower"; hero span "One shelf."→"One bower." (echoes wordmark's "...Bower" ending); CTA "Start your shelf"→"Build your bower". ⊥ touch already-correct literal GameBower copy (T42) or Nest Hex visual assets (T43). Decorative bookshelf-spine bar (`spines` array + 2 render blocks, purely visual shelf motif, ⊥ text) ? out of scope this task — flag only, restyle later if wanted. Test: welcome.vue has ⊥ "shelf" substring left in template copy.|V59,T42,T43,§C.brand
T62|x|welcome hero decorative bar still literal old-brand visual (flagged T61, ⊥ fixed there — copy-only): `spines` array + `border-b-4` render block draws a bookshelf (vertical spines sitting on a shelf ledge) — bookshelf metaphor never redesigned for Nest Hex identity, same leftover-imagery class as B20 but visual ⊥ text. Replace w/ Nest Hex motif: row of small hexagon tiles (outline | filled, varying size mirrors current skyline rhythm) across brand palette (teal `#2FA7A0`, slate `#3A4556`, violet `#7D6BFF`, indigo `#1A1E4A`) — echoes BrandMark's nested-hex shape, ⊥ shelf-ledge baseline border. ⊥ touch welcome.vue copy (T61 done) or BrandMark/BrandWordmark components (T43, reused as-is elsewhere) — this decorative element is welcome-page-local, standalone markup/data (`spines`-equivalent array), ⊥ a shared component change.|§C.brand,T43,T61
T63|x|Xbox platform connect + sync (V60): Azure AD app reg → real MS OAuth login (login.microsoftonline.com) → 3-hop token exchange (MS access token → XBL3.0 user token @ user.auth.xboxlive.com → XSTS token @ xsts.auth.xboxlive.com, relying party xboxlive.com), mirrors community xbox-webapi/xboxreplay pattern; `platform_connections.platform` gains `xbox`; queued sync job (mirrors SteamSyncer/GogSyncer shape) fetches Xbox Live `titlehub` title-history (games w/ Live activity/achievements — ⊥ full MS Store purchase history, §C caveat, playtime nullable mirrors GOG V12), upserts owned_games (V10 key, V24-style reconcile); reuses existing canonical matching (V7/V4 IGDB match, ⊥ new matching logic) + union/facets/badges (already platform-agnostic, iterate entry.platforms[] generically — ⊥ new FE surface work expected beyond connect form + platform enum in StoreConnectionRequest); token refresh flow for XSTS expiry (mirrors GOG V14); env XBOX_CLIENT_ID/XBOX_CLIENT_SECRET (§I). 3rd distinct auth shape (OAuth2 token-exchange chain vs GOG's plain OAuth2 vs Steam's OpenID-identity+APIkey) — validates connector abstraction holds for a 3rd shape. Tests mirror T6 (GOG connect, multi-platform dedupe validation): connect flow, sync ingestion, token refresh, resync upsert/reconcile.|V60,V7,V10,V14,V24,I.xbox,§C

## §B bugs

id|date|cause|fix
B1|2026-07-13|browser "CORS" error local = no server bound to apiBase http://localhost:8000; Herd site `gameshelf` pointed at `web/.output/public` ⊥ `api/public`; Laravel CORS defaults fine (ACAO *)|Herd relink → api/, web/.env apiBase, §C.dev-topology
B2|2026-07-14|T20 dev: V22 unspecified for remote removal — pull-absent + gog_present cleared flag → push re-added → remote write thrash. Caught by idempotency test pre-commit|V22 extended
B3|2026-07-14|SteamClient::getOwnedGames set `include_played_free_games=1` → Steam auto-included F2P titles user never chose ("anyone technically owns" per Steam docs)|V23
B4|2026-07-14|SteamSyncer only ever upserted, ⊥ removed rows absent from fresh fetch → V23 fix stops new noise but existing stale rows (B3, + any genuinely-removed games) persist forever|V24
B5|2026-07-15|GameMatcher::match() ⊥ try/catch around searchGame — early syncs (before Twitch creds configured) threw on first attempt, aborting rest of batch → 529/770 games stuck provisional (no cover) w/ no retry path. Live-verified 19/20 sample matches instantly once creds valid|V26
B6|2026-07-15|GameMatcher searched raw Steam title only — titles w/ ®™© (LEGO® Worlds, Titanfall® 2) never matched IGDB's glyph-free canonical titles, stayed provisional forever (no retry differs by title, cache MISS permanent)|V27
B7|2026-07-15|IgdbClient::CANONICAL_FIELDS queried `age_ratings.category,age_ratings.rating` — IGDB overhauled this schema, fields renamed to `organization`+`rating_category` (nested); old names silently dropped by IGDB (no error) → `esrb_rating` never populated despite T27's own "? confirm live @ build" flag never actually being resolved. Live-verified via age_ratings/age_rating_organizations/age_rating_categories endpoints|V37
B8|2026-07-15|SyncLibraryIgdb monolithic — 1 job serial-loops whole library, 2 IGDB calls/game (getGame+timeToBeat) @ ≤4 req/s → 770-game library ≈ 6+ min, exceeds queue timeout (retry_after 90s) → killed mid-batch, retry restarts from zero|V39
B9|2026-07-15|LibraryFilterSidebar renders IGDB game_modes facet values ("Multiplayer","Co-operative","Split screen") alongside dedicated V32 bool filters (Multiplayer/Co-op/Local multiplayer/Local co-op) → same concept appears twice in sidebar. T22 facet × T27 flags overlap, ⊥ data bug|V40
B10|2026-07-15|esrb[] validation hardcoded enum E,E10,T,M,AO,RP but esrb_rating stores IGDB labels verbatim ("E10+" ≠ "E10") → facet-emitted value 422s on round-trip ("The selected esrb.0 is invalid."); pre-T36 select sent E10 → silently matched ∅ forever. I.api contradicted own I.ext age_ratings line (already documented "E10+"). Same class B7: IGDB shape assumed ⊥ live-verified|V43
B11|2026-07-15|IgdbClient::gameIdFromExternal + externalUid query external_games by `category` — IGDB overhauled external_games, renamed `category` → `external_game_source` (id ref). Old `category = 1\|5` filter silently returns `[]` (200, no error) → every steam/gog wishlist external-id lookup misses → all wishlist imports land provisional (igdb_id null, no cover/genres). 61/61 local wishlist rows provisional, all origin=steam. Live-verified: uid 620 (Portal 2) matches `external_game_source = 1`, empty under `category = 1`; source ids unchanged (1=Steam, 5=GOG). Same class B7/B10: IGDB field assumed ⊥ live-verified, wrong name fails silent|V45
B13|2026-07-15|rematch (T29) 404s on wishlist-only games: RematchController owns-check (user->ownedGames()->exists()) + GameRematch::apply repoint scoped owned_games only (V34), but T38/V42 union surfaces wishlist_items + meta-orphans in /api/library & FE FixMatchPanel (games/[id].vue) renders for every union entry (incl B11 provisional wishlist rows) → FE offers fix-match, API rejects. V34 was written owned-only pre-union|V34 broadened → T47
B15|2026-07-15|SyncLibraryIgdb ("sync all IGDB" bulk refresh, T31/T32) fans out owned-only: matchedGameIds() enumerates $user->ownedGames() join games (refresh half), MatchConnectionIgdb runs GameMatcher::matchConnection (owned provisionals) per connection (match half). Wishlist-only + meta-orphan games (T38/V42 union, incl B11 provisional wishlist rows) never matched nor refreshed → bulk "sync all IGDB" silently ignores wishlist games. Same class B13 (rematch owned-only pre-union)|V48 → T50
B14|2026-07-15|T46 regression: WishlistSyncService::pullSteam absence reconciliation keyed on resolved game_id/$seen. T46 made a transient IGDB/Steam 500 return null (per-item skip) instead of aborting → still-present appid drops from $seen → handleRemoteAbsence reaps its wishlist row as "remotely absent" (data loss). Pre-T46 the exception aborted whole sync so reconciliation never ran. GOG unaffected (keys absence on raw productIds, V22). Proven: transient-500 test deleted a still-wishlisted row|V47
B12|2026-07-15|WishlistSyncService::gameFromExternal per-item resolution not fault-tolerant: gameIdFromExternal (IGDB/Twitch) or appName fallback (Steam store 500 — aggressive unauth rate-limit, same endpoint as deck-compat noise §C.T26) throws uncaught → whole SyncWishlist job aborts, unlike GameMatcher (V26). Compounded by B11: category miss forces appName fallback for every appid, maximizing store-500 exposure. Also negative mapping cached forever (0) → no retry after IGDB adds mapping. Log line 1389: earlier same-method abort on Twitch 400|V46
B16|2026-07-15|wishlisted-game IGDB resync fails "too many tries" (MaxAttemptsExceededException): fan-out jobs MatchGameIgdb (T50) + RefreshGameIgdb + MatchConnectionIgdb attach RateLimited('igdb-sync') = Limit::perSecond(2) GLOBAL (⊥ ->by() key) but set no $tries/retryUntil → Laravel default tries=1. RateLimited over-budget → $job->release($delay), and a release COUNTS as an attempt → 3rd+ job dispatched in any 1s window released past ceiling → job dies before handle() ever runs matchGame. T50's per-game wishlist fan-out (1 job per provisional wishlist/meta game, ≤61 from B11) floods the 2/sec limiter → mass "too many tries"; owned path (1 MatchConnectionIgdb per connection) rarely exceeds 2/sec ∴ latent since T50. Fix = time-bounded retries (retryUntil), ⊥ count ceiling|V52 → T53
B17|2026-07-15|GOG login "invalid_request: No client id supplied": FE builds auth.gog.com authorize URL (buildGogAuthUrl, connections.ts) from config.public.gogClientId (nuxt.config.ts), sourced from NUXT_PUBLIC_GOG_CLIENT_ID w/ silent ''-fallback if unset. web/.env lacks the key; no web/.env.example exists to document it (unlike api/.env.example, which documents GOG_CLIENT_ID/GOG_CLIENT_SECRET). §C env list only covered API-side vars, omitted the FE's own copy. ProfileConnections.vue renders "Log in to GOG" unconditionally, no empty-clientId guard → link always live, dead-ends at GOG's server w/ opaque error instead of failing loud locally. Same class B7/B10/V43 (silent misconfig → opaque external error)|V54 → T56
B18|2026-07-15|GOG login code rejected after successful GOG login: user copies from `embed.gog.com/on_login_success?origin=client&code=...&session_id=...` and pastes into the "GOG login code" field; FE sent the raw paste verbatim (`gogCode.value.trim()`) to `connect()` → `GogClient::exchangeCode`, ⊥ any extraction. Any imprecise copy (whole URL, extra `&session_id=...`, un-decoded `%XX` in the code) reaches GOG's token endpoint malformed → 400, surfaced as generic "GOG rejected that login code" — same symptom for several distinct input mistakes, no diagnostic signal which. Manual substring-extraction from a URL is inherently error-prone (same class as any copy-paste OAuth flow lacking a real redirect handler, T6's community-workaround design)|V55 → T57
B19|2026-07-15|GOG login still rejected after T57 (extraction fix): root cause was NOT the paste — direct probe against auth.gog.com/token (bogus code, bypassing any FE/user variable) returned `invalid_client: The client credentials are invalid`, not `invalid_grant`, proving the credential PAIR itself was wrong. api/.env's `GOG_CLIENT_SECRET` was 63 chars — 1 short of the genuine 64-char secret (dropped trailing digit when transcribed from memory in conversation, unverified against any source at the time). Corrected + re-probed → invalid_grant (credentials now accept, only the bogus placeholder code fails, as expected). Compounding factor: GogClient::requestToken discarded the actual GOG error body on 400/401 (returned bare null) — the generic "code rejected" message was IDENTICAL whether cause was bad credentials or bad code, costing a full guess-and-check round trip that server-side logging would've shortcut to one look|V56 → T58
B20|2026-07-16|welcome.vue (T42 rebrand) swapped literal `GameShelf`→`GameBower` occurrences only (header logo, hero body sentence, footer) — missed 3 "shelf" metaphor spots woven into copy that name the old concept w/o spelling the old brand string: step title "One shelf", hero span "One shelf.", CTA "Start your shelf". Landing page (only public marketing surface, §C.landing) still read like the old product. Same failure class as B7/B10/B11 (rebrand assumed literal-string-only scope, silently incomplete — a grep for "GameShelf" passed clean, "shelf" alone as leftover metaphor was never checked)|V59 → T61
